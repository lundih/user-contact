server:
  port: 10001

  error:
    include-message: always
    include-exception: true

spring:
  datasource:
    url: jdbc:h2:mem:contacts
    username: sa
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update